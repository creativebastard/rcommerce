[build]
# Default build settings

[build.env]
passthrough = [
    "SQLX_OFFLINE",
    "CARGO_TERM_COLOR",
]

# You can add custom images here if needed
# [target.x86_64-unknown-linux-gnu]
# image = "ghcr.io/cross-rs/cross:x86_64-unknown-linux-gnu"
